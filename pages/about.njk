---
layout: page
title: About me
customClass: page-about
icon: "/img/nav/about.svg"
permalink: /about/
---

{# Intro #}
<div class="container ex-normal d-flex d-flex-wrap">
  <div class="intro">
  {% markdown %}
  I'm __Thi__, a Vietnamese __Data Scientist__ who has a curious mind. As an never-stop-learning worm, I am interested in researching and sharing contents about Applied Mathematics and Data Science. I have a Ph.D. degree in Applied Mathematics at [U. Sorbonne Paris Nord](https://www.math.univ-paris13.fr){:target="_blank"} and a very enthusiastic self-learner in Data Science.

  I use this website to document everything I've learned while going through the process of learning and working in this field.
  {% endmarkdown %}
  </div>
  <div class="coordinate">
    {% markdown %}
  - <span>From</span> Vietnam
  - <span>Age</span> 30
  - <span>Location</span> Île-de-France, France
  - <span>Github</span> github.com/dinhanhthi
  - <span>LinkedIn</span> linkedin.com/in/dinhanhthi
  - <span>Email</span> dinhanhthi@gmail.com
    {% endmarkdown %}
		{% if metadata.look4job %}
			<div class="job">
				<span>Available for a full-time job!</span>
			</div>
		{% endif %}
  </div>
  <p class="contact">
    <a class="button blue no-effect" href="mailto:dinhanhthi@gmail.com">Hire me</a>
    <a class="button black" href="{{ '/' | url }}">🖊 My notes</a>
		<a class="button black" href="https://photos.app.goo.gl/9OVEkdTjmtRPg7vC3" target="_blank">🎨 My sketches</a>
		<a class="button black" href="/reading/">📚 Reading list</a>
  </p>
</div>

{# Skills #}
<div class="container d-flex d-flex-wrap wide">
  {% for group in skills %}
    <div id="{{ group.name | lower }}" class="section skills">
      <div class="div-h2">
        <h2>{{ group.name }}</h2>
      </div>
      <div class="list-of">
        {% for item in group.list %}
          {% if not item.later %}
            <div class="badge black">
              <img src="{{ item.icon }}" alt="{{ item.name }}" height="20" width="20">
              <span>{{ item.name }}</span>
            </div>
          {% endif %}
        {% endfor %}
      </div>
    </div>
  {% endfor %}
</div>

{# Education & Experiences #}
<div class="container d-flex d-flex-wrap wide">
  {% for sec in cv %}
    <div id="{{ sec.name | lower }}" class="section exp-edu">
      <div class="div-h2">
        <h2 class="d-flex align-items-center"><img src="{{ sec.icon }}" alt="{{ sec.name }}" height="30" width="30">
          {{ sec.name }}</h2>
      </div>
      <div class="list-of">
        {% for item in sec.list %}
          <div class="item">
            <div class="d-flex d-flex-wrap align-items-center">
              <div class="where">
                <div>
                  {% if item.url %}
                    <a href="{{ item.url }}" target="_blank" rel="noopener noreferrer">{{ item.where }}</a>
                  {% else %}
                    {{ item.where }}
                  {% endif %}
                </div>
                <div>{{ item.date }}</div>
              </div>
              <div class="title">
                <h4>{{ item.title }}</h4>
                <div class="where-2">
                  <span>{% if item.url %}<a href="{{ item.url }}" target="_blank" rel="noopener noreferrer">{{ item.where }}</a>{% else %}{{ item.where }}{% endif %}</span>,
                  <span class="date-2">{{ item.date }}</span>
                </div>
                <p>{{ item.activity }}</p>
                <div class="tech">
                  {% for item2 in item.tech %}
                    <div title="{{ item2.name }}" class="skill-small">
                      <img src="{{ item2.icon }}" alt="{{ item2.name }}" height="20" width="20">
                    </div>
                  {% endfor %}
                </div>
              </div>
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  {% endfor %}
</div>

{# Projects #}
<div class="container wide">
  <div id="projects" class="section projects">
    <div class="div-h2">
      <h2 class="d-flex align-items-center">
        Projects
      </h2>
    </div>
    <div class="list-of-all d-flex d-flex-wrap">
      {% for item in projects %}
        <div class="list-wrapper">
          <div class="list-of">
            <div class="title">
              <a href="{% if item.source %}{{ item.source }}{% else %}{{ item.url }}{% endif %}">
                <h4>{{ item.icon }}
                  <span>{{ item.title }}</span></h4>
              </a>
              <p>{{ item.description | safe }}</p>
              {% if item.tech or item.tech_text %}
                <div class="tech">
                  {% if item.tech %}
                    {% for item2 in item.tech %}
                      <div title="{{ item2.name }}" class="skill-small">
                        <img src="{{ item2.icon }}" alt="{{ item2.name }}" height="20" width="20">
                      </div>
                    {% endfor %}
                  {% endif %}
                  {% if item.tech_text %}
                    {% for item3 in item.tech_text %}
                      <span class="tech-text">{{ item3 }}</span>
                    {% endfor %}
                  {% endif %}
                </div>
              {% endif %}
            </div>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</div>