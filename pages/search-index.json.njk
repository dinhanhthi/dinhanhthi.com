---
permalink: pages/search-index.json
---
{# use with external posts #}
{# {% set newPostListSearch = [] %}
{% for post in collections.posts %}
	{% if not post.data.hide %}
		{% set newPostListSearch = (newPostListSearch.push({url: post.url, title: post.data.title, keywords: post.data.keywords, tags: post.data.tags, cat: post.data.tags, target: "_self"}), newPostListSearch) %}
	{% endif %}
{% endfor %} #}

{# {% for item in categories %}
	{% if cat_external_post[item.name] %}
		{% for post in cat_external_post[item.name] %}
			{% if not post.hide %}
				{% set newPostListSearch = (
					newPostListSearch.push({url: post.url, title: post.title, keywords: post.keywords, cat: item.name, target: "_blank"}), newPostListSearch) %}
			{% endif %}
		{% endfor %}
	{% endif %}
{% endfor %} #}

{# {{ newPostListSearch | search | dump | safe | striptags(true) | escape }} #}

{{ collections.posts | search | dump | safe | striptags(true) | escape }}