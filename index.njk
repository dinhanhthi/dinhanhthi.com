---
title: "Hi! I'm Thi ツ"
layout: page
customClass: "page-index"
description: I failed my way to success
icon: '/img/avatar.svg'
date: Last Modified
---

<header class="pt-0">
  {% markdown %}
  I'm a Vietnamese guy who has a curious mind. I'd like to learn something new everyday. I'm a PhD in Applied Maths and currently a __Data Scientist__. You find in this site my notes which are taken when I've found something new in __Data Science__ and __Web Developement__.{:.intro}
  {% endmarkdown %}
  <div class="social">
    {% for item in social %}
      <a class="no-effect" title="{{ item.name }}" href="{{ item.url }}">
        <img src="{{ item.icon }}" alt="{{ item.name }}" height="40" width="40">
      </a>
    {% endfor %}
  </div>
  <p class="job mt-1p5 mb-1p5">
    <span>Available for a full-time job!</span>
  </p>
  <p class="contact mt-0p5">
    <a class="button blue no-effect" href="mailto:dinhanhthi@gmail.com">Hire me</a>
    <a class="button black" href="{{ '/about/' | url }}">My CV</a>
  </p>
</header>

<div class="container wide d-flex d-flex-wrap">
  <div class="main-cats">
    {% for item in categories %}
      <div class="category-wrapper">
        <div class="category">
          <h2 id="{{ item.name | lower | trim | replace(" ", "-") }}">
            {% if item.logo %}{{ item.logo }}
            {% elif item.svg %}<img src="{{ item.svg }}" alt="{{ item.name }}" height="30" width="30">{% endif %}{{ item.name }}
          </h2>
          {% set postslist = collections[item.name] %}
          {% set customClass = 'list-homepage' %}
          {% include "postslist.njk" %}
        </div>
      </div>
    {% endfor %}
  </div>
  <div class="toc-index">
    <h3>Notes by subjects</h3>
    <ul class="toc">
      {% for item in categories %}
        <li>
          <a href="#{{ item.name | lower | trim | replace(" ", "-") }}">{{ item.name }}</a>
        </li>
      {% endfor %}
    </ul>
  </div>

  <p style="font-style: italic; color: #777; font-size: 0.95rem; margin: 0; order: 3;">
    <sup style="color: #bbb">•</sup>{{ other_data.notfull }}
  </p>
</div>