---
title: "Hi! I'm Thi ツ"
layout: layouts/page.njk
header: homepage
customClass: "page-index"
description: I failed my way to success
logo:
  path: '/img/avatar.svg'
  alt: "Thi's avatar"
date: Last Modified
---

<div class="container wide d-flex d-flex-wrap">
  {% for item in categories %}
    <div class="category">
      <h2>{% if item.logo %}{{ item.logo }} {% elif item.svg %}<img src="{{ item.svg }}" alt="{{ item.name }}" height="30" width="30">{% endif %}{{ item.name }}</h2>
      {% set postslist = collections[item.name] %}
      {% set customClass = 'list-homepage' %}
      {% include "postslist.njk" %}
    </div>
  {% endfor %}
</div>