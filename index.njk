---
title: "Hi! I'm Thi :)"
layout: layouts/page.njk
header: homepage
description: I failed my way to success
image: https://upload.wikimedia.org/wikipedia/en/a/a9/Example.jpg
date: Last Modified
eleventyNavigation:
  key: "🏠 Home"
  order: 1
---

<div class="container-flex">
  {% for tag in categories %}
    <div class="cat-flex">
      <h2>{% if tag.logo %}{{ tag.logo }} {% elif tag.svg %}<img src="{{ tag.svg }}" alt="r-lang" height="30" width="30">{% endif %}{{ tag.name }}</h2>
      {% set postslist = collections[tag.name] %}
      {% set customClass = 'list-homepage' %}
      {% include "postslist.njk" %}
    </div>
  {% endfor %}
</div>